(()=>{const toast=document.createElement('div');toast.className='toast';toast.setAttribute('role','status');toast.setAttribute('aria-live','polite');document.body.appendChild(toast);let timer;window.atrToast=(message)=>{toast.textContent=message;toast.classList.add('show');clearTimeout(timer);timer=setTimeout(()=>toast.classList.remove('show'),2600)};document.querySelectorAll('[data-toast]').forEach(el=>el.addEventListener('click',()=>window.atrToast(el.dataset.toast)));document.querySelectorAll('[data-demo-import]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const input=form.querySelector('input');if(!input.value.trim()){window.atrToast('Paste an Airbnb, Vrbo, or Booking.com listing URL first.');input.focus();return}window.atrToast('Demo property imported — opening your preliminary score.');setTimeout(()=>location.href='/prototype/host/',900)}));document.querySelectorAll('[data-save]').forEach(el=>el.addEventListener('click',()=>window.atrToast('Prototype changes saved.')));document.querySelectorAll('[data-toggle]').forEach(el=>el.addEventListener('click',()=>{el.classList.toggle('active');window.atrToast(el.classList.contains('active')?'Added to guest guide.':'Removed from guest guide.')}));})();